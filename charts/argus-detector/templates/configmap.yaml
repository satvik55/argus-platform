apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "argus-detector.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "argus-detector.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
