alertmanager:
  enabled: false

prometheus-pushgateway:
  enabled: false

kube-state-metrics:
  enabled: false

prometheus-node-exporter:
  enabled: false

server:
  resources:
    requests:
      memory: 64Mi
    limits:
      memory: 128Mi

  persistentVolume:
    enabled: false